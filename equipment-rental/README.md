# Equipment Rental Management System

Step 1 establishes the React + Vite client and Express server foundation.

## Run locally

```bash
cd client
npm run dev
```

In another terminal:

```bash
cd server
npm run dev
```

The client runs on `http://localhost:5173` and the API runs on `http://localhost:5000`.
